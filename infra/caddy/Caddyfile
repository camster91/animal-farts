# Canonical Caddyfile fragment for the animal-farts PWA.
# Synced to /opt/caddy/Caddyfile on the host by scripts/sync-caddy.sh.
# Sibling deploys (alinenasseh / jwhabits / tkd / arcan-painting)
# overwrite the live file and wipe the animals block; the sync
# script re-asserts it.
#
# This fragment is appended to the END of the live Caddyfile so
# the arcan apex block (also re-asserted by a sister script) lives
# before it in source order, but Caddy doesn't care about order.

# Animal Farts PWA (PootBox) — re-asserted 2026-06-13.
animals.ashbi.ca {
  reverse_proxy 127.0.0.1:3015
}
